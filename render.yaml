services:
  - type: web
    name: luma-finance
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:server --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9